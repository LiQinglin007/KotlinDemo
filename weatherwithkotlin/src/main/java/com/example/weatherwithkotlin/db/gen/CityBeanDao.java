package com.example.weatherwithkotlin.db.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.example.weatherwithkotlin.db.bean.CityBean;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "CITY_BEAN".
*/
public class CityBeanDao extends AbstractDao<CityBean, Long> {

    public static final String TABLENAME = "CITY_BEAN";

    /**
     * Properties of entity CityBean.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property CityBeanId = new Property(0, Long.class, "CityBeanId", true, "_id");
        public final static Property CityName = new Property(1, String.class, "CityName", false, "CITY_NAME");
        public final static Property Sign = new Property(2, Integer.class, "Sign", false, "SIGN");
    };


    public CityBeanDao(DaoConfig config) {
        super(config);
    }
    
    public CityBeanDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"CITY_BEAN\" (" + //
                "\"_id\" INTEGER PRIMARY KEY ," + // 0: CityBeanId
                "\"CITY_NAME\" TEXT," + // 1: CityName
                "\"SIGN\" INTEGER);"); // 2: Sign
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"CITY_BEAN\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, CityBean entity) {
        stmt.clearBindings();
 
        Long CityBeanId = entity.getCityBeanId();
        if (CityBeanId != null) {
            stmt.bindLong(1, CityBeanId);
        }
 
        String CityName = entity.getCityName();
        if (CityName != null) {
            stmt.bindString(2, CityName);
        }
 
        Integer Sign = entity.getSign();
        if (Sign != null) {
            stmt.bindLong(3, Sign);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, CityBean entity) {
        stmt.clearBindings();
 
        Long CityBeanId = entity.getCityBeanId();
        if (CityBeanId != null) {
            stmt.bindLong(1, CityBeanId);
        }
 
        String CityName = entity.getCityName();
        if (CityName != null) {
            stmt.bindString(2, CityName);
        }
 
        Integer Sign = entity.getSign();
        if (Sign != null) {
            stmt.bindLong(3, Sign);
        }
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public CityBean readEntity(Cursor cursor, int offset) {
        CityBean entity = new CityBean( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // CityBeanId
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // CityName
            cursor.isNull(offset + 2) ? null : cursor.getInt(offset + 2) // Sign
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, CityBean entity, int offset) {
        entity.setCityBeanId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setCityName(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setSign(cursor.isNull(offset + 2) ? null : cursor.getInt(offset + 2));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(CityBean entity, long rowId) {
        entity.setCityBeanId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(CityBean entity) {
        if(entity != null) {
            return entity.getCityBeanId();
        } else {
            return null;
        }
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
